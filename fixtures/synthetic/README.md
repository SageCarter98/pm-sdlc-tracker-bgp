# Synthetic fixtures

Everything under this directory is fictional. No real organisation, person,
email address, document or payment detail may be added here — this is the
Directive v1.2 Section 18 boundary that authorises local development before
independent review, licensing clearance and production approval exist
(REQ-052, DEC01–DEC03).

`tenants.json` provides the two-tenant fixture set described in
`docs/blueprint/Blueprint_Working_Source.md` Section 6.1 ("First working
increment"): two fictional tenants, each with a sponsor, contributor and
approver, for exercising cross-tenant isolation once WP03/WP04 exist. It has
no application code to consume it yet — it is a data contract fixed ahead of
that work, not a claim that isolation is implemented.
